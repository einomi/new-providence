{% macro menu(className, items) %}
	<nav class="{{ className }} menu" style="text-align: center;" role="navigation">
		{% for item in items %}
			<a href="{{ item.path }}" class="menu__item link">{{ item.name }}</a>
		{% endfor %}
	</nav>
{% endmacro %}
